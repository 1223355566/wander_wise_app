import 'package:flutter/material.dart';

class Auth extends StatelessWidget {
  static const String routeName = "AutsScreen";
  const Auth({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
